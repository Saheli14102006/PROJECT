<h1> QR Code Generator </h1>
<p> Simplest code in Python.<br>
You have to install it using -> pip install qrcode pillow (VS code).
If your VS code terminal shows error type this instead -> python -m pip install qrcode pillow.
</p>